import { ServicesTypePipe } from './services-type.pipe';

describe('ServicesTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ServicesTypePipe();
    expect(pipe).toBeTruthy();
  });
});
